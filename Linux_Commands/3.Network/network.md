# Network Commands

- `ip addr` — display network interfaces and IP addresses
- `ping host` — test connectivity to `host`
- `ss -tuln` — list listening TCP and UDP ports
- `curl URL` — make an HTTP request or download data from a URL
- `wget URL` — download a file from a URL
- `nslookup domain` — look up the IP address of a domain
- `traceroute host` — display the path packets take to reach `host`