# Search Commands

- `find path -name "name"` — search for files or directories by name
- `grep "pattern" file` — search for text matching `pattern` in a file
- `locate name` — quickly search for files by name using the locate database
- `which command` — display the path to an executable command