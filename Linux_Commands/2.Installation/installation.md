# Installation Commands

- `./configure` — configure the source code for your system and generate build files
- `make` — compile the source code into executable programs
- `make install` — install the compiled program onto your system